Public Class frmprueba
    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click
        Dim conn As New conexion
        'conn.Probarconexion()
        'grdatos.DataSource = conn.consulta(grdatos, "select * from cliente")
    End Sub
End Class
